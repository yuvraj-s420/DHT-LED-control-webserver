# DHT-LED-control-webserver
A webserver that is auto updated with websockets, and uses JSON to transmit data to client. Temperature and Humidity data is updated on the server every 1 second interval, and an led can be controlled through buttons on the html site. Ensure pins are set to your circuit, and replace ssid and password with your own wifi name and password.
